<Page x:Class="Julie.UI.Views.ChatView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Messages Area -->
        <ScrollViewer Grid.Row="0" Margin="10">
            <ListView x:Name="MessagesListView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="LightGray" 
                               Margin="5" 
                               Padding="10">
                            <StackPanel>
                                <TextBlock Text="Role" FontWeight="Bold"/>
                                <TextBlock Text="Message content" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        
        <!-- Input Area -->
        <StackPanel Grid.Row="1" 
                   Orientation="Horizontal" 
                   Margin="10">
            <TextBox x:Name="MessageTextBox" 
                    PlaceholderText="Type your message..."
                    Width="300"
                    Margin="0,0,10,0"/>
            <Button Content="Send"/>
            <Button Content="Record" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Page>
